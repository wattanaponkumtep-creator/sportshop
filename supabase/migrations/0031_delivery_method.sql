-- =========================================================================
-- 0031: วิธีจัดส่ง (ขนส่ง) ของงาน — เพื่อบอกโรงงานว่าส่งด้วยอะไร
-- เช่น รถทัวร์, Kerry, Flash, ไปรษณีย์ ฯลฯ
-- =========================================================================

alter table public.jobs
  add column if not exists delivery_method text;
